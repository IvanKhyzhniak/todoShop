<%=
    sanitize({  user: { email: @user.email, 
                        full_name: @user.full_name,
                        finished_orders_count: @user.orders.where(status: :accepted).count
                },
                session: {
                  auth_token: @session.auth_token
                }
            }.to_json )       
%>